<?xml version="1.0" encoding="utf-8" ?>
<manifest
  xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:tools="http://schemas.android.com/tools"
>
  <uses-feature android:name="android.hardware.camera" />
  <uses-feature android:name="android.hardware.camera.autofocus" />
  <uses-feature android:name="android.hardware.audio.output" />
  <uses-feature android:name="android.hardware.microphone" />
  <uses-feature
    android:name="android.hardware.telephony"
    android:required="false"
  />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission
    android:name="android.permission.BIND_TELECOM_CONNECTION_SERVICE"
    tools:ignore="ProtectedPermissions"
  />
  <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
  <!-- notification on android 13 -->
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.CALL_PHONE" />
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
  <uses-permission
    android:name="android.permission.MODIFY_PHONE_STATE"
    tools:ignore="ProtectedPermissions"
  />
  <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
  <!-- react-native-webrtc bluetooth -->
  <uses-permission
    android:maxSdkVersion="30"
    android:name="android.permission.BLUETOOTH"
  />
  <uses-permission
    android:maxSdkVersion="30"
    android:name="android.permission.BLUETOOTH_ADMIN"
  />
  <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
  <uses-permission
    android:maxSdkVersion="29"
    android:name="android.permission.READ_PHONE_STATE"
  />
  <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
  <uses-permission android:name="android.permission.REORDER_TASKS" />
  <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
  <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
  <uses-permission android:name="android.permission.VIBRATE" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
  <uses-permission
    android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"
  />

  <!-- android >= 14 background permissions -->
  <!-- Required so the ongoing-call foreground service keeps microphone (and,
       for video calls, camera) access while the app is in the background
       (while-in-use permission model). phoneCall type alone does NOT grant
       background microphone/camera access. The camera type is only applied at
       runtime when the CAMERA permission is granted (see VoiceConnectionService). -->
  <uses-permission
    android:minSdkVersion="34"
    android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE"
  />
  <uses-permission
    android:minSdkVersion="34"
    android:name="android.permission.FOREGROUND_SERVICE_CAMERA"
  />

  <!-- call history -->
  <!-- TODO: temporary disabled
  <uses-permission android:name="android.permission.WRITE_CALL_LOG" />
  <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS" />
  -->

  <!-- callkeep -->
  <uses-permission
    android:minSdkVersion="34"
    android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL"
  />

  <!-- android lpc -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission
    android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"
  />

  <application
    android:allowBackup="false"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:largeHeap="true"
    android:name=".MainApplication"
    android:networkSecurityConfig="@xml/network_security_config"
    android:requestLegacyExternalStorage="true"
    android:supportsRtl="true"
    android:theme="@style/AppTheme"
  >
    <!-- orientation is controlled at runtime: portrait while ringing, user
         rotation once the in-call react surface is shown. configChanges keeps
         rotation from recreating the activity, which would tear down the
         mounted react surface mid call -->
    <activity
      android:autoRemoveFromRecents="true"
      android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
      android:exported="true"
      android:label="IncomingCallActivity"
      android:launchMode="standard"
      android:name=".activity.IncomingCallActivity"
      android:showOnLockScreen="true"
      android:windowSoftInputMode="adjustPan"
    />
    <activity
      android:autoRemoveFromRecents="true"
      android:label="ExitActivity"
      android:name=".activity.ExitActivity"
      android:theme="@android:style/Theme.NoDisplay"
    />

    <!-- react-native-notifications -->
    <meta-data
      android:name="com.google.firebase.messaging.default_notification_icon"
      android:resource="@mipmap/ic_launcher"
    />
    <meta-data
      android:name="com.google.firebase.messaging.default_notification_channel_id"
      android:value="default"
    />

    <!-- custom push notification handler -->
    <service
      android:enabled="true"
      android:exported="true"
      android:foregroundServiceType="remoteMessaging"
      android:name=".push_notification.BrekekeMessagingService"
      tools:ignore="ForegroundServicePermission"
    >
      <intent-filter>
        <action android:name="com.google.firebase.MESSAGING_EVENT" />
      </intent-filter>
    </service>

    <!-- call keep -->
    <service
      android:name="io.wazo.callkeep.VoiceConnectionService"
      android:label="Wazo"
      android:exported="false"
      android:permission="android.permission.BIND_TELECOM_CONNECTION_SERVICE"
      android:foregroundServiceType="phoneCall|microphone|camera"
      tools:ignore="ForegroundServicePermission"
    >
      <intent-filter>
        <action android:name="android.telecom.ConnectionService" />
      </intent-filter>
    </service>
    <service
      android:name="io.wazo.callkeep.RNCallKeepBackgroundMessagingService"
      android:foregroundServiceType="phoneCall|microphone|camera"
    />

    <!-- react dev -->
    <activity
      android:name="com.facebook.react.devsupport.DevSettingsActivity"
    />

    <!-- main -->
    <!-- android:supportsPictureInPicture="true" for Picture-in-Picture of react-native-video latest version -->
    <!-- portrait by default: only the call manage page unlocks rotation via
         BrekekeUtils.setOrientation while it is visible -->
    <activity
      android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
      android:exported="true"
      android:label="@string/app_name"
      android:launchMode="singleTask"
      android:name=".MainActivity"
      android:screenOrientation="portrait"
      android:supportsPictureInPicture="true"
      android:taskAffinity=""
      android:theme="@style/SplashTheme"
      android:windowSoftInputMode="adjustResize"
    >
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
      <!-- default phone call app -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <action android:name="android.intent.action.DIAL" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:scheme="tel" />
      </intent-filter>
      <intent-filter>
        <action android:name="android.intent.action.DIAL" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
      <!-- deep link -->
      <intent-filter>
        <action android:name="android.intent.action.VIEW" />
        <category android:name="android.intent.category.DEFAULT" />
        <category android:name="android.intent.category.BROWSABLE" />
        <data android:host="open" android:scheme="brekekephonedev" />
        <data android:host="call" android:scheme="brekekephonedev" />
        <data android:host="custompage" android:scheme="brekekephonedev" />
      </intent-filter>
    </activity>

    <!-- default phone call app -->
    <service
      android:exported="true"
      android:name=".call_history.CallService"
      android:permission="android.permission.BIND_INCALL_SERVICE"
    >
      <meta-data
        android:name="android.telecom.IN_CALL_SERVICE_UI"
        android:value="true"
      />
      <intent-filter>
        <action android:name="android.telecom.InCallService" />
      </intent-filter>
    </service>

    <!-- call history -->
    <!-- TODO: temporary disabled
    <receiver android:name="BrekekeOutgoingCallHandler" android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.NEW_OUTGOING_CALL" />
      </intent-filter>
    </receiver>
    -->

    <!-- lpc -->
    <service
      android:enabled="true"
      android:exported="true"
      android:foregroundServiceType="specialUse"
      android:name=".lpc.BrekekeLpcService"
    >
      <property
        android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
        android:value="Brekeke Phone is an enterprise VoIP/PBX client. When the user enables the fallback Local Push Connectivity (LPC) option, the app maintains a persistent TLS socket to the user’s configured on-premise Brekeke PBX server so the user can receive real-time incoming VoIP call alerts and chat messages when Firebase Cloud Messaging is unavailable, blocked, or unreliable due to enterprise firewall/network restrictions. This task cannot be deferred or interrupted without material user impact: incoming VoIP calls may not ring in real time, calls may be missed, and PBX chat messages may be delayed. WorkManager or periodic polling cannot replace this because the feature requires a continuous bidirectional socket for immediate server-initiated delivery, not deferred background work. The service is user-initiated by enabling the LPC/push fallback option, is user-perceptible through a persistent foreground service notification, and can be stopped anytime by turning off the option in Account Settings. The service stops immediately when the option is disabled."
      />
    </service>
    <service
      android:enabled="true"
      android:name=".lpc.BrekekeLpcServiceIntent"
    />
    <receiver
      android:enabled="true"
      android:exported="false"
      android:name=".lpc.BrekekeLpcReceiver"
    >
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.ACTION_SHUTDOWN" />
        <action android:name="android.intent.action.REBOOT" />
        <category android:name="android.intent.category.DEFAULT" />
      </intent-filter>
    </receiver>
  </application>
</manifest>
