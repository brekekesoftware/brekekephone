import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  CustomGradient: {
    zIndex: 1,
    position: 'relative',
    width: '100%',
    height: '100%',
  },
})

export default styles
