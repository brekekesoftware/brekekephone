import * as Rn from 'react-native';

Rn.TouchableNativeFeedback.Ripple = color => color;

// Put these words to chrome dev tool log filter to remove unwanted logs
// -props.style -intentionally -conditionally -value.toString
